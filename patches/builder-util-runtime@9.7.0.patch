diff --git a/out/CancellationToken.d.ts b/out/CancellationToken.d.ts
index 3ec280da54ebbb4778622386af97b62a36be38e4..8a44ede8a10a2ba3970330d402f96d1b3b3157e8 100644
--- a/out/CancellationToken.d.ts
+++ b/out/CancellationToken.d.ts
@@ -8,7 +8,7 @@ export declare class CancellationToken extends EventEmitter {
     constructor(parent?: CancellationToken);
     cancel(): void;
     private onCancel;
-    createPromise<R>(callback: (resolve: (thenableOrResult: R | PromiseLike<R>) => void, reject: (error: Error) => void, onCancel: (callback: () => void) => void) => void): Promise<R>;
+    createPromise<R>(callback: (resolve: (thenableOrResult: R | PromiseLike<R>) => void, reject: (error: Error) => void, onCancel: (callback: () => void | Promise<void>) => void) => void): Promise<R>;
     private removeParentCancelHandler;
     dispose(): void;
 }
diff --git a/out/CancellationToken.js b/out/CancellationToken.js
index c639742e4f63c8cd7e2062513764da40a176e594..e0d1538d2de4d2367beb5754a2f1423d8cd38d9c 100644
--- a/out/CancellationToken.js
+++ b/out/CancellationToken.js
@@ -52,23 +52,38 @@ class CancellationToken extends events_1.EventEmitter {
         let cancelHandler = null;
         return new Promise((resolve, reject) => {
             let addedCancelHandler = null;
+            let cancelling = false;
             cancelHandler = () => {
+                if (cancelling) {
+                    return;
+                }
+                cancelling = true;
                 try {
                     if (addedCancelHandler != null) {
-                        addedCancelHandler();
+                        const cleanup = addedCancelHandler();
                         addedCancelHandler = null;
+                        if (cleanup != null) {
+                            Promise.resolve(cleanup).then(() => reject(new CancellationError()), reject);
+                            return;
+                        }
                     }
                 }
-                finally {
-                    reject(new CancellationError());
+                catch (error) {
+                    reject(error);
+                    return;
                 }
+                reject(new CancellationError());
             };
             if (this.cancelled) {
                 cancelHandler();
                 return;
             }
             this.onCancel(cancelHandler);
-            callback(resolve, reject, (callback) => {
+            callback(value => {
+                if (!cancelling) resolve(value);
+            }, error => {
+                if (!cancelling) reject(error);
+            }, (callback) => {
                 addedCancelHandler = callback;
             });
         })
diff --git a/out/httpExecutor.d.ts b/out/httpExecutor.d.ts
index ca97e7b03f79d1fcdfcb94cb8f8377c4aacab51b..ff2f78306f8685842b239955fc1dce2182d6aeb1 100644
--- a/out/httpExecutor.d.ts
+++ b/out/httpExecutor.d.ts
@@ -58,7 +58,7 @@ export declare abstract class HttpExecutor<T extends Request> {
 }
 export interface DownloadCallOptions {
     responseHandler: ((response: IncomingMessage, callback: (error: Error | null) => void) => void) | null;
-    onCancel: (callback: () => void) => void;
+    onCancel: (callback: () => void | Promise<void>) => void;
     callback: (error: Error | null) => void;
     options: DownloadOptions;
     destination: string | null;
diff --git a/out/httpExecutor.js b/out/httpExecutor.js
index 8d9114761ab0cf92bf56668e8cac8139bc0d71d5..9dcbb7fc76e59c7f82ab211d493fbfa6bd3554cd 100644
--- a/out/httpExecutor.js
+++ b/out/httpExecutor.js
@@ -240,14 +240,16 @@ Please double check that your authentication token is correct. Due to security r
         });
     }
     doDownload(requestOptions, options, redirectCount) {
+        let cancelPipes = null;
         const request = this.createRequest(requestOptions, (response) => {
+            if (options.options.cancellationToken.cancelled) {
+                response.destroy();
+                return;
+            }
             if (response.statusCode >= 400) {
                 options.callback(new Error(`Cannot download "${requestOptions.protocol || "https:"}//${requestOptions.hostname}${requestOptions.path}", status ${response.statusCode}: ${response.statusMessage}`));
                 return;
             }
-            // It is possible for the response stream to fail, e.g. when a network is lost while
-            // response stream is in progress. Stop waiting and reject so consumer can catch the error.
-            response.on("error", options.callback);
             // this code not relevant for Electron (redirect event instead handled)
             const redirectUrl = safeGetHeader(response, "location");
             if (redirectUrl != null) {
@@ -260,12 +262,19 @@ Please double check that your authentication token is correct. Due to security r
                 return;
             }
             if (options.responseHandler == null) {
-                configurePipes(options, response);
+                cancelPipes = configurePipes(options, response);
             }
             else {
+                response.on("error", options.callback);
                 options.responseHandler(response, options.callback);
             }
         });
+        const closed = new Promise(resolve => request.once("close", resolve));
+        options.onCancel(() => {
+            const pipesClosed = cancelPipes == null ? undefined : cancelPipes();
+            request.abort();
+            return Promise.all([closed, pipesClosed]).then(() => {});
+        });
         this.addErrorAndTimeoutHandlers(request, options.callback, requestOptions.timeout);
         this.addRedirectHandlers(request, requestOptions, options.callback, redirectCount, requestOptions => {
             this.doDownload(requestOptions, options, redirectCount++);
@@ -474,20 +483,13 @@ function configurePipes(options, response) {
     }
     const fileOut = (0, fs_1.createWriteStream)(options.destination);
     streams.push(fileOut);
-    let lastStream = response;
-    for (const stream of streams) {
-        stream.on("error", (error) => {
-            fileOut.close();
-            if (!options.options.cancellationToken.cancelled) {
-                options.callback(error);
-            }
-        });
-        lastStream = lastStream.pipe(stream);
-    }
-    fileOut.on("finish", () => {
-        ;
-        fileOut.close(options.callback);
-    });
+    const closed = new Promise(resolve => fileOut.once("close", resolve));
+    (0, stream_1.pipeline)(response, ...streams, options.callback);
+    return () => {
+        for (const stream of streams) stream.destroy();
+        response.destroy();
+        return closed;
+    };
 }
 function configureRequestOptions(options, token, method) {
     if (method != null) {
